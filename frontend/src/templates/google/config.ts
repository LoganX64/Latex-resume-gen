import type { TemplateConfig } from '@/types/resume'

export const googleConfig: TemplateConfig = {
  id: 'google',
  name: 'Google Style',
  description: 'Clean, structured layout inspired by Google engineering resumes. Clear hierarchy with bolded metrics.',
  supportsPhoto: false,
  category: 'modern',
}
