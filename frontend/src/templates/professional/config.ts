import type { TemplateConfig } from '@/types/resume'

export const professionalConfig: TemplateConfig = {
  id: 'professional',
  name: 'Professional ATS',
  description: 'Clean, traditional single-column layout optimized for ATS scanning. Based on Jake\'s Resume with pipe-separated contact info.',
  supportsPhoto: false,
  category: 'classic',
  margins: { top: 12.7, bottom: 12.7, left: 12.7, right: 12.7 },
}
